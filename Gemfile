source 'https://rubygems.org'

# require 'json'
# require 'open-uri'
# versions = JSON.parse(open('https://pages.github.com/versions.json').read)

gem 'github-pages', '88' # versions['github-pages']
gem 'kramdown', '1.11.1' # versions['kramdown']
gem 'redcarpet', '3.3.3' # versions['redcarpet']
